if SHIELD_TRIPLE_LEFT

config ZMK_KEYBOARD_NAME
    default "Triple"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_TRIPLE_LEFT || SHIELD_TRIPLE_RIGHT

config ZMK_SPLIT
    default y

endif
